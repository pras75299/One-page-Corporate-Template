    <section class="great-started section-padding">
        <div class="container">
            <div class="row">
                <div class="col-md-12 col-sm-12">
                    <div class="great-started-text text-center">
                        <h4><%= (getStarted && getStarted.heading) ? getStarted.heading : 'We Are Ready to Help You' %></h4>
                        <h2><%= (getStarted && getStarted.subheading) ? getStarted.subheading : 'Get the Best Solution for Your Business' %></h2>
                        <a class="get-start-btn smooth-scroll" href="<%= (getStarted && getStarted.button_url) ? getStarted.button_url : '#home' %>"><%= (getStarted && getStarted.button_text) ? getStarted.button_text : 'Get started' %></a>
                    </div>
                </div>
            </div>
        </div>
    </section>
